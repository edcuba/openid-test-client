<!DOCTYPE html>
<html lang="en">
<head>
    <% include ./partials/head %>
</head>
<body class="container">
  <header>
      <% include ./partials/header %>
  </header>

  <main>
    <div class="row">
        <% if (!logged) { %>
            <div class="col-sm-12">
                <div class="jumbotron">
                    <h2>Log into the service</h2>
                    <br/>
                    <form action="/login" method="GET">
                        <input class="btn btn-primary btn-lg" type="submit" role="button" value="Log in"/>
                    </form>
                </div>
            </div>
        <% } %>
    </div>
    <% if (logged) { %>
        <div class="well">
            <h3>User data</h3>
            <pre id="json"><%= userdata %></pre>
        </div>
        <div class="well">
            <h3>Claims</h3>
            <pre id="json"><%= claims %></pre>
        </div>
        <div class="well">
            <h3>State</h3>
            <pre id="json"><%= state %></pre>
        </div>
    <% } %>
  </main>

  <% include ./partials/footer %>
</body>
</html>